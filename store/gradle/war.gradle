apply plugin: "war"

bootWar {
    mainClass = "com.okta.developer.store.StoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}

if (project.hasProperty("e2e")) {
    tasks.named('bootWar') {
        archiveFileName = 'e2e.war'
    }
}
