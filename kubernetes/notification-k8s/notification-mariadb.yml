apiVersion: v1
kind: Secret
metadata:
  name: notification-mariadb
  namespace: default
  labels:
    app: notification-mariadb
type: Opaque
data:
  mariadb-root-password: YTZjMWVmMDkwYzBjMjgzNTBlNmVmYWExMzJlZmQ5ZjU5NTBiOGI0Y2ZiOTk0OGYzYTE3NTVjOWE2YmQ5
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-mariadb
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-mariadb
  template:
    metadata:
      labels:
        app: notification-mariadb
    spec:
      volumes:
        - name: data
          emptyDir: {}
      containers:
        - name: mariadb
          image: mariadb:12.3.2
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: notification-mariadb
                  key: mariadb-root-password
            - name: MYSQL_DATABASE
              value: notification
          #        command:
          #        - mysqld
          #        - --lower_case_table_names=1
          #        - --skip-ssl
          #        - --character_set_server=utf8mb4
          #        - --explicit_defaults_for_timestamp
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql/
          resources:
            requests:
              memory: '512Mi'
              cpu: '500m'
            limits:
              memory: '1Gi'
              cpu: '1'
---
apiVersion: v1
kind: Service
metadata:
  name: notification-mariadb
  namespace: default
spec:
  selector:
    app: notification-mariadb
  ports:
    - port: 3306
